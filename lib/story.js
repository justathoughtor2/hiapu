/*jslint devel: true, node: true*/
var express = require('express');

function storyStudy(doc) {
    switch(doc.id) {
        case 0: {
            break;
        }
        default: {
            console.log('Hello world!');
        }
    }
}